[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "conda-bencher-baremetal"
version = "0.1.0"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["tests"]

[tool.pytest.ini_options]
addopts = "--benchmark-disable-gc"
